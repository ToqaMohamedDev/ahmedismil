#!/bin/bash

# Script لإعداد المشروع

echo "🚀 بدء إعداد المشروع..."

# تثبيت المتطلبات
echo "📦 تثبيت المتطلبات..."
npm install

# نسخ ملف .env.example إذا لم يكن موجوداً
if [ ! -f .env.local ]; then
    echo "📝 إنشاء ملف .env.local..."
    cp .env.example .env.local 2>/dev/null || echo "⚠️  يرجى إنشاء ملف .env.local يدوياً"
fi

# إنشاء مجلدات إضافية إذا لزم الأمر
echo "📁 إنشاء المجلدات..."
mkdir -p public/uploads
mkdir -p .next

echo "✅ تم إعداد المشروع بنجاح!"
echo ""
echo "📋 الخطوات التالية:"
echo "1. أضف بيانات Firebase في ملف .env.local"
echo "2. شغّل المشروع: npm run dev"
echo "3. افتح http://localhost:3000"

